/**
 * Tags every course-home case that authors content in a worker course and reads
 * it back as a learner: they run in `studio-author` as the worker's author, and
 * need Studio.
 */
export const AUTHORED_COURSE_HOME_TAGS: string[] = ['@studio', '@author', '@mfe-learning'];
